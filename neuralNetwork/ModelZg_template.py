import numpy as np

"""
This is the template for the ModelZg function used in the simulation
"""
def ModelZg(x):
    """
    Zg = ModelZg([Z1,...,ZN,P,T])
    The function ModelZg takes input x of dimension Q x (Number of components + 2)
    and returns output y of dimension Q x 1 where
    Q is the number of samples,
    Z1,...,ZN is the overall component mass fractions in the vapor,
    T is its temperature before flashing,
    P is its pressure before flashing,
    Zg is the compressibility factor of the vapor
    """

    return 

